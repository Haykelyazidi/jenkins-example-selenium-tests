pipeline {
  agent any
  stages {
    stage('Verify browsers are installed') {
      steps {
       
      }
    }
    stage('Run Tests') {
      steps {
        sh 'mvn clean test'
      }
    }
  }
}
